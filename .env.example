# ================================================================
# Mini-Ecommerce Backend - Environment Variables
# ================================================================
# Copy this file to `.env` and adjust values as needed.
# These values are used by docker-compose and services via environment variables.

# ----------------------------------------------------------------
# PostgreSQL Database Configuration
# ----------------------------------------------------------------
# Credentials used by all PostgreSQL database containers:
# - user-db (users_db:5432)
# - product-db (products_db:5433)
# - order-db (orders_db:5434)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# ----------------------------------------------------------------
# API Gateway Security
# ----------------------------------------------------------------
# API key for accessing protected endpoints through the Gateway
# Include this as 'X-API-Key' header in all API requests
# Example: curl -H "X-API-Key: example-api-key-12345" http://localhost:8080/api/products
API_KEY=example-api-key-12345

# ----------------------------------------------------------------
# Config Server (Optional - Git Backend)
# ----------------------------------------------------------------
# By default, Config Server uses native file system (config-repo/)
# IT'S BETTER TO USE GITHUB REPOSITORY BECAUSE IT MIGHT NOT WORK LOCALLY
# Uncomment these to use a Git repository instead
# Note: Git backend is recommended for production environments
# GIT_URI=https://github.com/your-username/your-config-repo.git
# GIT_USERNAME=your-github-username
# GIT_PASSWORD=your-github-personal-access-token

# ----------------------------------------------------------------
# Email Configuration (Notification Service)
# ----------------------------------------------------------------
# Gmail SMTP credentials for sending email notifications
# IMPORTANT: Use Gmail App Password, NOT your regular account password!
# To generate an app password:
#   1. Go to https://myaccount.google.com/security
#   2. Enable 2-Step Verification
#   3. Go to https://myaccount.google.com/apppasswords
#   4. Generate a new app password for "Mail"
#   5. Use the 16-character password here
EMAIL_USERNAME=your-email@gmail.com
EMAIL_PASSWORD=your-16-char-app-password
EMAIL_PORT=587
EMAIL_ENABLED=true


# ----------------------------------------------------------------
# Kafka Configuration (Advanced - Usually no changes needed)
# ----------------------------------------------------------------
# Kafka and Zookeeper are pre-configured in docker-compose.yml
# Topics are auto-created: order-events, product-events, user-events
# Kafka UI available at: http://localhost:8090
