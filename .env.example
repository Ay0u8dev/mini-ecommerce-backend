# Copy this file to `.env` and adjust values as needed.
# These values are used by docker-compose and services via environment variables.

# --- PostgreSQL credentials used by user-db, product-db, order-db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# --- Optional: Use Git backend for Config Server (instead of native)
# IT IS RECOMMENDED TO USE GIT FOR CONFIG SERVER BECAUSE IT MAY NOT WORK PROPERLY WITH NATIVE FILE SYSTEM
# Uncomment and set these if you configure the Config Server to use a Git repo
# GIT_URI=https://github.com/{username}/your-config-repo.git
# GIT_USERNAME=(your github username)
# GIT_PASSWORD=(get personal access token from github/settings/tokens)

# --- API Gateway simple API key
# If you externalize it via env, wire it in your Gateway config/filter accordingly
API_KEY=example-api-key-12345

# --- Email (Notification Service)
# Gmail SMTP credentials for sending emails
EMAIL_USERNAME=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_PORT=587
EMAIL_ENABLED=true
